name: Dev Deploy

on:
  workflow_dispatch:

jobs:
  uses: RGupta0323/ReusableWorkflows/.github/workflows/DevDeploy.yml@master
  build:
    environment: Dev
    runs-on: ubuntu-latest
    steps:
      

